# Local generated Python output
__pycache__/
*.pyc

# Restaurant data — private (fetched from eater-map-data at build) + generated
static/data/restaurants.raw.json
static/data/restaurants.json

# Scraped Eater data & pipeline DBs — kept in the private eater-map-data repo
/data/
/data-pipeline/raw-downloads/
/data-pipeline/archive/
/data-pipeline/sitemap.csv

# Build and dependency output
node_modules/
.pnpm-store/
.svelte-kit/
.vercel/
dist/
build/

# Logs and local runtime files
*.log
dev-server*.log
*.sqlite-shm
*.sqlite-wal

# Local env (Protomaps API key etc.) — set VITE_PROTOMAPS_KEY here / in Vercel
.env
.env.local
.env.*.local

# Local Codex/agent state
.agents/
.codex/
.local-tools/

# Every change gets its own worktree under here, so each one is a checkout of this
# repository inside itself -- a `git add -A` that catches one commits it as a gitlink
# no clone can resolve. settings.local.json is this machine's permission mode, which
# is nobody else's. The guard itself stays TRACKED: .claude/settings.json,
# .claude/hooks/ and .claude/worktree-per-change.json, because a worktree only gets a
# file if git puts it there.
.claude/worktrees/
.claude/settings.local.json
