# Local generated Python output
__pycache__/
*.pyc

# Restaurant data — private (fetched from eater-map-data at build) + generated
static/data/restaurants.raw.json
static/data/restaurants.json

# Build and dependency output
node_modules/
.pnpm-store/
.svelte-kit/
.vercel/
dist/
build/

# Logs and local runtime files
*.log
dev-server*.log
*.sqlite-shm
*.sqlite-wal

# Local env (Protomaps API key etc.) — set VITE_PROTOMAPS_KEY here / in Vercel
.env
.env.local
.env.*.local

# Local Codex/agent state
.agents/
.codex/
.local-tools/
